{
  "name": "satellite-doppler",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
  "dev": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "deploy": "npm run build && gh-pages -d dist"
},
  "dependencies": {
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
