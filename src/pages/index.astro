---
import BaseLayout from '../layouts/BaseLayout.astro';
import { withBase } from '../utils/path';
---

<BaseLayout title="Accueil – Kalifa Sankara">
  <section class="hero">
    <div class="hero-text">
      <h1 class="hero-title">Dr. Kalifa Sankara</h1>
      <p class="hero-subtitle">
        Intelligence artificielle, agriculture durable & transformation scientifique des territoires.
      </p>
      <a href="/kalifa-sankara/presentation" class="button">En savoir plus</a>
    </div>
    <div class="hero-image">
      <img src={withBase("images/pp_kalifa_1.PNG")} alt="Kalifa Sankara" />
    </div>
  </section>

  <section class="intro">
    <h2>Un pont entre technologie et impact humain</h2>
    <p>
      Kalifa Sankara œuvre à la convergence entre données, agriculture et éducation. Ses projets mêlent IA, énergies renouvelables et politiques publiques pour un avenir durable en Afrique et ailleurs.
    </p>
  </section>

  <style>
    .hero {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      gap: 2rem;
      padding: 4rem 0;
    }

    .hero-text {
      flex: 1 1 350px;
    }

    .hero-title {
      font-size: 3rem;
      font-weight: 800;
      margin: 0 0 1rem;
      background: linear-gradient(90deg, var(--color-accent-blue), var(--color-accent-green));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .hero-subtitle {
      font-size: 1.3rem;
      color: var(--color-text);
      margin-bottom: 2rem;
    }

    .hero-image {
      flex: 1 1 250px;
      text-align: center;
    }

    .hero-image img {
      max-width: 240px;
      border-radius: 50%;
      border: 4px solid var(--color-accent-red);
      box-shadow: 0 0 15px var(--color-accent-red);
    }

    .intro {
      margin-top: 3rem;
    }

    .intro h2 {
      font-size: 1.8rem;
      color: var(--color-accent-green);
      margin-bottom: 1rem;
    }

    .intro p {
      font-size: 1.1rem;
      line-height: 1.6;
    }

    @media (max-width: 768px) {
      .hero {
        flex-direction: column;
        text-align: center;
      }
      .hero-title {
        font-size: 2.4rem;
      }
      .hero-image img {
        max-width: 180px;
      }
    }
  </style>
</BaseLayout>
