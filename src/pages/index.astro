---
import BaseLayout from '../layouts/BaseLayout.astro';
import { withBase } from '../utils/path';
---

<BaseLayout title="Accueil – Kalifa Sankara">
  <section class="banner">
    <div class="banner-content">
      <div class="text">
        <h1>Kalifa Sankara</h1>
        <p>Retrouvez ici toute mon actualité en exclusivité</p>
      </div>
      <div class="photo">
        <img src={withBase('images/pp_kalifa_1.PNG')} alt="Kalifa Sankara" />
      </div>
    </div>
  </section>

  <section class="main-grid">
    <aside class="left-column">
      <div class="login-card">
        <h3>Connexion</h3>
        <p>Veuillez vous connecter pour aimer et commenter mon actualité</p>
        <a href="https://linkedin.com" class="btn linkedin">LinkedIn</a>
        <a href="https://facebook.com" class="btn facebook">Facebook</a>
        <p class="confidentialite"><a href="#">Termes de Confidentialité</a></p>
      </div>
    </aside>

    <main class="content-column">
      <article class="news-card">
        <div class="news-author">
          <img src={withBase('images/pp_kalifa_1.PNG')} alt="Kalifa Sankara" />
          <div>
            <strong>Kalifa Sankara</strong> · <span>2 min</span>
          </div>
        </div>
        <h3>Fini le temps des regrets – En avant pour un nouveau départ !</h3>
        <p>Qui n’a jamais eu envie, un jour, de tout abandonner et de disparaître ? Par peur d’être rejeté encore une fois, par crainte d’échouer... <a href="#">Lire la suite</a></p>
      </article>
    </main>

    <aside class="right-column">
      <div class="sponsor-card">
        <h4>À la une</h4>
        <img src={withBase('images/sponsor.png')} alt="Sponsorisé" />
        <p>Participez au Forum des Leaders d’Afrique de l’Ouest</p>
        <a class="btn gold" href="#">Découvrir</a>
      </div>
    </aside>
  </section>

  <style>
    :root {
      --bg-light: #f0f2f5;
      --text-dark: #1c1e21;
      --blue: #1877f2;
      --linkedin: #0077b5;
      --gold: #f5c518;
    }

    body {
      background: var(--bg-light);
      color: var(--text-dark);
      font-family: 'Inter', sans-serif;
    }

    .banner {
      width: 100%;
      padding: 3rem 0;
      background: linear-gradient(90deg, #5a7593, #1a2632);
      color: white;
      box-sizing: border-box;
    }

    .banner-content {
      max-width: 800px;
      margin: auto auto;
      padding: 0 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 2rem;
    }

    .banner-content .text {
      flex: 1 1 60%;
    }

    .banner-content .text h1 {
      font-size: 2.6rem;
      margin-bottom: 0.5rem;
      color: #ffffff;
    }

    .banner-content .text p {
      font-size: 1.15rem;
      color: #cfd8dc;
    }

    .banner-content .photo {
      flex: 1 1 35%;
      text-align: right;
    }

    .banner-content .photo img {
      max-width: 100%;
      height: auto;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
      object-fit: cover;
    }

    .main-grid {
      display: grid;
      grid-template-columns: 280px 1fr 280px;
      gap: 2rem;
      padding: 2rem;
      max-width: 1400px;
      margin: auto;
      background: var(--bg-light);
    }

    .left-column,
    .right-column {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .login-card,
    .sponsor-card,
    .news-card {
      background: white;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      padding: 1.5rem;
    }

    .btn {
      display: block;
      margin-top: 1rem;
      padding: 0.7rem;
      border-radius: 8px;
      font-weight: bold;
      text-align: center;
      text-decoration: none;
      color: white;
    }

    .btn.linkedin {
      background: var(--linkedin);
    }

    .btn.facebook {
      background: var(--blue);
    }

    .btn.gold {
      background: var(--gold);
      color: black;
      margin-top: 1rem;
    }

    .confidentialite {
      font-size: 0.85rem;
      margin-top: 1rem;
    }

    .confidentialite a {
      color: var(--blue);
    }

    .news-card img {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      object-fit: cover;
    }

    .news-author {
      display: flex;
      gap: 1rem;
      align-items: center;
      margin-bottom: 1rem;
    }

    .news-card h3 {
      margin-top: 0;
    }

    .news-card p {
      color: #444;
    }

    .sponsor-card img {
      width: 100%;
      border-radius: 8px;
      margin: 1rem 0;
    }

    @media (max-width: 1024px) {
      .main-grid {
        grid-template-columns: 1fr;
      }

      .left-column,
      .right-column {
        flex-direction: row;
        justify-content: space-between;
      }

      .banner-content {
        flex-direction: column;
        text-align: center;
      }

      .banner-content .photo {
        text-align: center;
      }
    }
  </style>
</BaseLayout>
